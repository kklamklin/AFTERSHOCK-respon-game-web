# คำสั่งสำหรับ AI ที่มาทำงานต่อในโปรเจกต์นี้

> อ่านไฟล์นี้ให้จบก่อนแตะโค้ด ผู้ใช้ไม่มีประสบการณ์เขียนโปรแกรม — AI เป็นคนเขียนโค้ดทั้งหมด

## โปรเจกต์นี้คืออะไร

**AFTERSHOCK** — เกมวางแผนกู้ภัยแผ่นดินไหว ทำส่งวิชาสังคมศึกษา (กำหนดส่ง 1 สัปดาห์)
ผู้เล่นเป็นผู้บัญชาการ ส่ง จนท. 4 เผ่าเข้าช่วยผู้รอดชีวิต 1,200 คน ใน 47 โซน ภายใน 72 ชั่วโมงในเกม (~10 นาทีจริง)

- แพลตฟอร์ม: เว็บ HTML/CSS/JS ล้วน **ไม่มี framework ไม่มี build step ไม่มี backend**
- บังคับ **แนวนอน (Landscape)** เน้นเล่นบนมือถือ
- ปลายทาง: **GitHub Pages** — เจ้าของเคาะแล้วว่าเอาแค่ให้เพื่อนเล่นได้ ไม่ต้องส่งไฟล์ให้ใคร
  ~~ไฟล์ HTML ไฟล์เดียว~~ **ยกเลิกแล้ว** (เหตุผลอยู่หัวข้อ "ทำไมไม่ทำไฟล์เดียว" ด้านล่าง)
- เกณฑ์ที่ครูให้ความสำคัญ: ความคิดสร้างสรรค์ + การนำเสนอหน้าชั้น

## เอกสารสเปก — อ่านก่อนเขียนทุกครั้ง

| ไฟล์ | ใช้ตอนไหน |
|---|---|
| **`docs/GAMESCREEN_SPEC.md`** | **สเปกหน้าเกมหลัก — ทับทุกฉบับ เคาะกับเจ้าของแล้ว อ่านอันนี้ก่อนเสมอ** |
| `docs/AFTERSHOCKMASTER.md` | เอกสารรวมฉบับหลัก |
| `docs/gamesystemfinal.md` | ตัวเลข สูตร บาลานซ์ โครงสร้างข้อมูล |
| `docs/gameflowspec.md` | Flow ผู้เล่น หน้าจอ UI **และกฎที่อัปเดตทับของเดิม** |

**อย่าอ่านทั้งเอกสารรวดเดียว** — เปิดเฉพาะหัวข้อที่เกี่ยวกับงานที่กำลังทำ ทุกไฟล์ stub มี comment บอกไว้แล้วว่าอ้างอิงหัวข้อไหน

### ⚠️ กฎที่ถูกแก้แล้ว — `gameflowspec.md` ทับ `gamesystemfinal.md` เสมอ

ถ้าสองเอกสารขัดกัน **ให้ยึด `gameflowspec.md`** จุดที่ต่างกันและพลาดกันบ่อย:

| หัวข้อ | ของเก่า (อย่าใช้) | **ของจริงที่ต้องใช้** |
|---|---|---|
| คำนวณผลตอนไหน | ตอนกด Deploy | **ตอน จนท. ทำงานเสร็จ** |
| คูลดาวน์คืออะไร | เวลาพักหลังทำงาน | **เวลาที่ จนท. กำลังทำงานอยู่ในโซน** |
| ผลลัพธ์ | 4 ระดับ (margin) | **2 ระดับ — สำเร็จ / ไม่สำเร็จ** |
| เคลียร์โซน | เฉพาะ "สำเร็จสูงสุด" | **สำเร็จ = เคลียร์เป็นเขียวเลย** |
| ลำดับการทอย | ทอยอันตรายก่อน | **ทอยสำเร็จก่อน — สำเร็จแล้วปลอดภัย 100% ไม่ทอยอันตรายเลย** |
| บัฟหลัง Deploy | ไม่มีผล | **มีผล** (เพราะคำนวณตอนจบงาน) |
| การสั่งงาน | กดเลือก 2 ครั้ง | **ลากวาง (Drag & Drop) ปล่อยคือยืนยัน ไม่มีหน้ายืนยัน** |

**%ช่วยชีวิตตอนสำเร็จ** (Q1b ที่เคยค้าง) → เคาะรอบสุดท้ายเป็น **ทอย 2 ชั้น** (ตารางระดับประจำตัว OP)
อยู่ใน `CONFIG.rescueTierChance` + `CONFIG.rescuePctRange` · ดู `docs/GAMESCREEN_SPEC.md` §4.2
*(วิธีเก่า `rescueRateMode = 'rollBetweenSuccessAnd100'` ถูกยกเลิกและลบออกจาก config แล้ว)*

## กฎการเขียนโค้ด

1. **ตัวเลขทุกตัวไปอยู่ที่ `src/config.js` เท่านั้น** — ห้าม hardcode ตัวเลขบาลานซ์กระจายในโค้ด เพราะต้องปรับหลังลองเล่น
2. **แยก logic ออกจาก UI** — `src/systems/` ห้ามแตะ DOM, `src/ui/` ห้ามคำนวณกฎเกม
3. **ES modules ล้วน** (`import`/`export`) ไม่มี bundler ตอน dev
4. **ไม่ติดตั้ง dependency ใด ๆ** — vanilla JS เท่านั้น
5. **คอมเมนต์เป็นภาษาไทย** อ้างอิงหัวข้อสเปก เช่น `// §5 การตายเชิงเส้น` เพราะเจ้าของโปรเจกต์อ่านโค้ดเองไม่ได้
6. **เก็บ trapped/casualty เป็นทศนิยม แสดงผลเป็นจำนวนเต็มปัดลง** (§5.5) กันเศษสะสมเพี้ยน

## เรื่อง asset — สำคัญ

รูปตัวละคร/สกิลจริง **ยังไม่เสร็จ ทยอยมาทีหลัง** ห้ามรอ ห้ามหยุดงาน

### ชื่อไฟล์ (คนละแบบกับเอกสารสเปกเดิม — ยึดอันนี้)

**ไฟล์จริงไม่ได้ตั้งตามสูตรเดียวกันทุกไฟล์** (ตัวพิมพ์เล็ก/ใหญ่ไม่ตรงกัน, บางไฟล์ใช้ `.png` บางไฟล์ `.PNG`, บางไฟล์ใช้ `-` บางไฟล์ใช้ `_` คั่นคำ)
เพราะงั้น **ห้ามเขียนสูตรสร้างชื่อไฟล์เอง** — ทุก path เก็บเป็น literal string ไว้ที่ `src/data/operators.js` (`portraits`, `skills[x].icon`, `STATUS_ICONS`) ที่เดียว ต้อง import ใช้จากตรงนั้นเสมอ

| สายพันธุ์ (key ใน OPERATORS) | ชื่อตัวละคร | ฝ่าย |
|---|---|---|
| `human` | Robertson | ภาคสนาม |
| `cat` | Lyla | ภาคสนาม |
| `elf` | Lia | ภาคฐาน |
| `spirit` | Mudongzock | ภาคฐาน |

- **ภาพตัวละคร:** `OPERATORS[key].portraits.{normal|injured|lost|talk}` — ภาคสนามมี normal/injured/lost, ภาคฐานมี normal/talk (talk เป็น array 2 เฟรมสลับกัน)
- **ไอคอนสกิล:** `OPERATORS[key].skills[skillId].icon`
- **ไอคอนสถานะ (ใช้ร่วมกันได้ทุกตัว):** `STATUS_ICONS.{injured|lost|cooldown}` — ยังไม่มีไฟล์จริง ใช้ placeholder ไปก่อน
- บางตัวจะมี **สกิลพิเศษเพิ่ม** ทีหลัง (`hasSpecialSkill` ใน operators.js) — **ยังไม่ต้องทำอะไรกับมันตอนนี้** รอสเปก/asset มาก่อน

**⚠️ ค้างแก้อยู่ (ดู comment `// TODO` ใน operators.js):**
- โฟลเดอร์ Icon สะกดชื่อตัว elf ว่า `Ria` แต่โฟลเดอร์ Operators สะกดว่า `Lia` — เจ้าของยืนยันว่า **Lia คือชื่อจริง** ไฟล์ไอคอนสกิลของตัวนี้ต้องเปลี่ยนชื่อเป็น Lia ตอนอัปจริง (ตอนนี้ path ในโค้ดยังอ้างชื่อเดิม `Ria` ตามไฟล์ที่มีอยู่ เพื่อไม่ให้ path พัง)
- ไฟล์สกิล spirit สะกดว่า `mudongzong` (เจ้าของยืนยันชื่อจริงคือ `mudongzock`) ต้องเปลี่ยนไฟล์ตอนอัปจริงเช่นกัน
- ยังไม่เห็นไฟล์: Robertson ท่า normal, Lyla ท่า finalstand (lost), เฟรมพูดของ Mudongzock — รอเจ้าของอัปเพิ่ม

ระหว่างนี้ใช้ placeholder (emoji / สี่เหลี่ยมสี) ที่**สลับเป็นรูปจริงได้โดยไม่ต้องแก้โค้ด** — asset จริงเข้ามาแค่วางทับชื่อไฟล์เดิมในโฟลเดอร์ `assets/`

- ✅ `map.svg` จริง (47 โซน) มาแล้วและต่อเข้า `src/ui/map.js` เรียบร้อย — id ใน SVG ตรงกับที่ `systems/zones.js` สร้าง 1:1
  การวาด/ซูม/ปัด อยู่ใน `src/ui/map.js` ที่เดียว ห้ามให้กระทบ `src/systems/`

## วิธีทำงานกับเจ้าของโปรเจกต์

- **ทำทีละส่วน อย่าทำรวดเดียวจบ** — เสร็จ 1 ส่วนที่ใช้งานได้จริง → commit → push
- **ทดสอบในเบราว์เซอร์จริงทุกครั้งก่อนบอกว่าเสร็จ** ใช้ skill `run` (Playwright อยู่ที่ `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`) ห้ามเดาว่าโค้ดทำงานถูก
- **อย่าลงมือก่อนถูกสั่ง** — เจ้าของจะสั่งเป็นส่วน ๆ (main menu, main game, ...) ให้ทำเฉพาะที่สั่ง
- อธิบายเป็นภาษาไทย สั้น ตรงประเด็น

### Git

- branch ที่ใช้: `claude/folder-structure-planning-0003vf`
- 1 commit = 1 ส่วนที่จบในตัว ไม่ commit งานครึ่ง ๆ กลาง ๆ
- push ทุกครั้งที่ commit
- **ห้ามเปิด PR เว้นแต่ถูกสั่ง**

### ตอนจบงาน

deploy ผ่าน **GitHub Pages** แล้วทำ QR code ให้เพื่อนสแกนเล่นบนมือถือ

**ขึ้นเว็บแล้ว → https://kklamklin.github.io/AFTERSHOCK-respon-game-web/**

⚠️ **repo ถูกเปลี่ยนชื่อจาก `game-web` เป็น `AFTERSHOCK-respon-game-web`**
แต่ `git remote` ยังต้องชี้ชื่อเดิม `github.com/kklamklin/game-web` — GitHub redirect ให้เอง
(ลองเปลี่ยนไปชื่อใหม่แล้ว push ไม่ผ่าน เพราะ proxy ของ session อนุญาตแค่ชื่อเดิม)

repo นี้มี branch เดียว (`claude/folder-structure-planning-0003vf`) ซึ่งเป็น default branch อยู่แล้ว
**Pages เสิร์ฟจาก branch นี้ตรง ๆ ไม่ต้อง merge ไปไหน**
Settings → Pages → Source: Deploy from a branch → branch นี้ + `/ (root)`

**push ขึ้น branch นี้เมื่อไหร่ เว็บอัปเดตเองใน 1-2 นาที**

### QR code — อยู่ที่ `qr/`

- `qr/AFTERSHOCKS-QR.png` — การ์ดพร้อมใช้ (ฉายขึ้นจอ / ปรินต์)
- `qr/qr-plain.svg` — เฉพาะตัว QR ขยายเท่าไหร่ก็คมเพราะเป็นเวกเตอร์

**สร้างด้วยโค้ดที่เขียนเอง** (`scratchpad/qr.py`) เพราะเครื่องนี้ไม่มีไลบรารี QR
ตรวจย้อนกลับด้วยการถอดรหัสจากตารางที่วาดเสร็จ + เช็ค Reed-Solomon syndrome ว่าเป็นศูนย์
**ถ้าเปลี่ยน URL ต้องสร้าง QR ใหม่** — QR ฝัง URL ไว้ข้างใน แก้ชื่อ repo แล้วอันเก่าจะพาไปหน้า 404

มี `.nojekyll` ที่ราก — กัน Jekyll ของ GitHub ไปยุ่งกับไฟล์ ต้องมีเสมอ ห้ามลบ

## ทำไมไม่ทำไฟล์เดียว (เคาะแล้ว)

วัดจริงก่อนตัดสินใจ: asset รวม **20 MB → 27.6 MB หลังแปลง Base64** (ฝังลงไฟล์เดียวทำให้ใหญ่ขึ้น 33% เสมอ)
และไฟล์เดียวต้องโหลดครบทั้งก้อนก่อนถึงจะขึ้นอะไรสักอย่าง ส่วนเว็บปกติเปิดหน้าปกได้ตั้งแต่ 334 KB

นอกจากใหญ่แล้วยัง **ต้องเขียนใหม่หลายจุด ไม่ใช่แค่เอาโค้ดมาต่อกัน**:
- โค้ด 34 ไฟล์ใช้ ES modules — `type="module"` ใช้ไม่ได้ตอนเปิดจาก `file://`
- `map.js` โหลดแผนที่ด้วย `fetch('map.svg')` — `fetch` ถูกบล็อกทั้งหมดบน `file://`
- `data/prefs.js` ใช้ localStorage — ถูกบล็อกบน `file://` ค่า Settings จะไม่ถูกจำ
- เพลง 11 MB ฝังเป็น data URI — มือถือบางรุ่นกินแรมหนักหรือเล่นไม่ออก

**ถ้าวันหลังเปลี่ยนใจอยากได้ไฟล์เดียวจริง ๆ ต้องแก้ 4 ข้อบนก่อน** และควรย่อเพลง/รูปให้เหลือราว 6 MB
(รูปตัวละครไฟล์ 1024px แต่โชว์จริงแค่ 30px — ย่อเป็น 256px ตาเปล่าดูไม่ออก)

## บังคับแนวนอน — หน้า "หมุนจอ"

markup อยู่ใน `index.html` (HTML นิ่ง ๆ ไม่ผ่าน JS จะได้ขึ้นทันทีแม้โค้ดยังโหลดไม่เสร็จ)
ซ่อน/แสดงด้วย `@media (orientation: portrait)` ใน `styles.css` อย่างเดียว

ตอนจอตั้ง `#screen-root` ถูกตั้ง `visibility: hidden` ด้วย — **จงใจ** ไม่ให้เกมเดินอยู่ข้างหลังโดยที่ผู้เล่นมองไม่เห็น
เทสอยู่ที่ `rotate.js`

## ลำดับงานที่วางไว้ (แบ่งเป็น 10 รอบ เคาะกับเจ้าของแล้ว)

หน้าเมนู/Settings/Intel/Tutorial เสร็จหมดแล้ว · ตารางนี้คือหน้าเกมหลัก

| # | งาน | สถานะ |
|---|---|---|
| 1 | โครง layout + HUD (AP · Time · ปุ่มเวลา 2 ปุ่ม · กล่อง Ⓐ/Ⓑ/Ⓒ) | ✅ |
| 2 | ฝัง `map.svg` จริง + ระบายสีตามระดับ + ซูม/ปัด | ✅ |

> เลขคนบนโซนถูกเอาออกทีหลัง (เจ้าของสั่ง) — ไปโผล่ในแผงล่างตอนลากแทน
| 3 | เดินเวลา 144 ลูป · คนตายจริง · AP เพิ่มจริง · กล่องล่างขยับตาม | ✅ |
| 4 | ป้ายสถานะ จนท. + คูลดาวน์บนไอคอนสกิล | ✅ |
| 5 | Drag & Drop ลงโซน (หยุดเวลาตอนลาก · กากบาทโซนที่ลงไม่ได้ · หัก AP) | ✅ |
| 6 | แผงข้อมูลตอนลาก — อัตราสำเร็จแยกส่วน + ความเสี่ยงบาดเจ็บ | ✅ |
| 7 | ครบเวลา → ทอย 2 ชั้น → rescued/casualty → โซนเขียว + Detail Feed | ✅ |
| 8 | Injured / Unconscious / CRITICAL / Last Stand | ✅ |
| 8.5 | **ปรับบาลานซ์ครั้งใหญ่** (หลังลองเล่นจริงแล้วเกมแพ้ทุกทาง) | ✅ |
| 9 | เมนู ☰ ในเกม + หน้าเวลาที่เหลือ + ป๊อปอัพหยุดเวลาอัตโนมัติ | ✅ |
| 10 | หน้า Result | ✅ |
| 10.1 | ~~build รวมไฟล์เดียว~~ | ❌ **ยกเลิก** — เจ้าของเลือก GitHub Pages |
| 10.2 | อนิเมชั่น (เฟดเปลี่ยนหน้า · เมนู · สกิล · Feed · กรอบเตือน) | ✅ |
| 10.3 | **เปลี่ยนระบบคะแนนเป็นแรงก์ 0–1200** | ✅ |
| 10.4 | เสียงในเกม (คลิก · ผลภารกิจ · เตือน 1 นาที · หน้า Result) | ✅ |
| 10.5 | **ปรับบาลานซ์รอบ 2** — กระจายคน · อายุโซน · เกณฑ์แรงก์ | ✅ |
| 10.6 | **ธีมหน้าจอสั่งการ** — น้ำเงินไฮเทค · ตึกเวกเตอร์ · แถบ % · ฟอนต์ | ✅ |
| 10.7 | **เพลงประจำฉาก + Settings ใช้ได้จริง** | ✅ |
| 10.8 | เตรียมขึ้น GitHub Pages (หน้าหมุนจอ · meta · favicon) | ✅ |
| 10.9 | **ขึ้นเว็บจริงแล้ว + QR code** | ✅ |
| 10.10 | **ล็อกอัตราส่วนจอ + รื้อ UI แถบ จนท. ให้เล่นบนมือถือได้จริง** | ✅ |
| 10.11 | **แก้เกมหนืดบน iOS** (วาดจอใหม่น้อยลง · ไม่โหลดเพลง 11 MB ตอนเปิดเกม) | ✅ |
| 10.12 | **ปรับบาลานซ์ Robertson** (Crowd Control ถูกลง+มีเงื่อนไข · Last Stand แรงขึ้นทั้งชุด) | ✅ |
| 10.13 | **QTE ต่อเวลา Last Stand** (มินิเกมจังหวะ · ตัดเพลง Stand Alone ออก) | ✅ |
| 10.14 | **คีย์ลัดสำหรับเล่นบนคอม** (Esc/P/T/เว้นวรรค/1-6) + อนิเมชั่นปิดจอ QTE | ✅ |

## คีย์ลัดสำหรับเล่นบนคอม (รอบ 10.14)

**การลากไอคอนแบบเดิมยังใช้ได้ทุกอย่าง** อันนี้เป็น "ทางที่สอง" ไม่ได้มาแทน (เจ้าของสั่งไว้ชัด)

| ปุ่ม | ทำอะไร |
|---|---|
| `Esc` | เปิดเมนู ☰ · ถ้ามีอะไรเปิดอยู่คือปิด · ถ้ากำลังเลือกสกิลอยู่คือยกเลิกการเลือก |
| `P` | หยุด / เดินเวลาต่อ |
| `T` | หน้าเวลาที่เหลือ |
| `เว้นวรรค` | ข้ามบทพูดในหน้าสอนเล่น (จบหน้านั้นแล้วปุ่มนี้ไม่ทำอะไรอีก — ถอด listener ทิ้งเลย) |
| `1` – `6` | เลือกสกิล: Robertson 1/2 · Lyla · Lia 1/2 · Mudongzock |

**วิธีใช้เลข 1-6:** กดเลข = เลือกสกิล → เอาเมาส์ไปคลิกโซนที่จะลง
กดเลขอื่นระหว่างนั้น = เปลี่ยนสกิล · **กดเลขเดิมซ้ำ = ยกเลิก** · คลิกนอกโซนที่ลงได้ = ยกเลิก (กติกาเดียวกับปล่อยนอกโซนตอนลาก)

- **Air Deploy (เลข 6) กดแล้วใช้เลย** ไม่ต้องเลือกโซน เพราะมันบัฟทั้งแมพ ไม่มีเป้าให้เลือกอยู่แล้ว
- ระหว่างเลือกสกิล **เวลาหยุด เพลงเล่นต่อ** เหมือนตอนลากเป๊ะ เพราะใช้กลไกเดียวกันทั้งหมด
  (`selectSkillByKey()` ใน `ui/dragdrop.js` ใช้ทางเดินเดียวกับการลาก ต่างแค่ "คลิก" มาแทน "ปล่อยนิ้ว")
- ตอนมินิเกม QTE เปิดอยู่ คีย์เกมทั้งหมดถูกปิด — มินิเกมมีคีย์ของตัวเอง **`Q` = ซ้าย · `E` = ขวา**
  (รับ `← →` · `A D` · `Z M` ด้วย เผื่อมือวางคนละที่ แต่คู่หลักคือ Q/E)

**เทสอยู่ที่ `scratchpad/keys.js`** (20 เคส) — แตะอะไรกับคีย์ลัดหรือการลากวางต้องรันซ้ำ

## QTE ต่อเวลา Last Stand (รอบ 10.13) — มินิเกมจังหวะ

**เวลา Last Stand หมด → ยังไม่ล้มทันที** เกมเปิดมินิเกมจังหวะขึ้นมาให้ต่อเวลาก่อน

เสี้ยวหัวใจวิ่งจากซ้ายและขวาเข้าหาหัวใจกลางจอ · กดปุ่ม **ฝั่งเดียวกับเสี้ยว** ตอนมันวิ่งมาทับพอดี
ในดราฟของเจ้าของมีปุ่มเดียว แต่ของจริงต้องแยกซ้าย-ขวา (เสี้ยวซ้ายรับด้วยปุ่มซ้ายเท่านั้น)

### ⚠️ โผล่ทีละรอบ ไม่ใช่เล่นรวดเดียว 3 รอบ

```
Last Stand 6 ชม. หมด  →  QTE 1 (20 จังหวะ)  → +2 ชม. → ปิดจอ เล่นเกมต่อ
2 ชม. นั้นหมด          →  QTE 2 (30 จังหวะ)  → +2 ชม. → ปิดจอ เล่นเกมต่อ
2 ชม. นั้นหมด          →  QTE 3 (40 จังหวะ)  → +2 ชม. → ปิดจอ เล่นเกมต่อ
2 ชม. สุดท้ายหมด       →  จบ ไม่มีต่อเวลาอีก (ล้มตามกฎเดิม)
```

| | |
|---|---|
| ผ่าน 1 รอบ | **+2 ชม.** · ครบ 3 รอบ = +6 ชม. (รวมเป็น 12 ชม. เท่าของเดิมแต่ต้องเล่นเก่ง) |
| จำนวนจังหวะ | รอบ 1 = **20** · รอบ 2 = **30** · รอบ 3 = **40** (คงที่ ไม่สุ่ม) |
| พลาดได้ | **3 ครั้งต่อรอบ · รีเซ็ตทุกรอบ** ไม่นับสะสมข้ามรอบ |
| พลาดครบ | จบทันที ไม่ได้เวลาของรอบนั้น และไม่มีรอบต่อไป (ล้มเลย) · รอบที่ผ่านมาแล้วยังได้เวลาไว้ |
| นับว่าพลาด | กดตอนไม่มีเสี้ยวอยู่ในกรอบ · กดผิดฝั่ง · ปล่อยให้เสี้ยววิ่งผ่านไปเฉย ๆ |
| เสียง | กดตรง `success-notification.mp3` · พลาด `op-down.mp3` |
| เล่นบนคอม | **`Q` = เสี้ยวซ้าย · `E` = เสี้ยวขวา** (ปุ่มบนจอยังกดได้ตามปกติ) |

**ตัวเลขทั้งหมดอยู่ที่ `CONFIG.qte`** — กรอบเขียว/เหลืองบนจอ **กว้างเท่ากับ `hitWindowMs` จริง ๆ**
(`ui/qte.js` คำนวณเป็นพิกเซลส่งให้ CSS) ผู้เล่นจึงเห็น "ช่วงที่กดทัน" ตรงกับกฎเป๊ะ ไม่ใช่วาดเอาสวย

- **ผ่านรอบแล้วมีอนิเมชั่นปิดจอ** — แผงวูบสว่างแล้วขยายจางหาย + วงแหวนกระเพื่อม (`.qte-wrap.is-out.is-win`)
  แพ้จะทรุดลงแล้วจางแทน · ความยาวต้องตรงกับ `CLOSE_MS` ใน `ui/qte.js` ไม่งั้นจอหายก่อนอนิเมชั่นจบ
- **เวลาในเกมหยุดตอนเล่น แต่เพลงเล่นต่อ** (ธง `bgmKeepPlaying` ตัวเดียวกับตอนลากไอคอน)
  เพราะจังหวะของมินิเกมอิงเวลาจริง/เพลง ไม่ได้อิงนาฬิกาเกม
- **`systems/` ไม่รู้จักมินิเกม** — มันแค่ส่งเหตุการณ์ `to: 'qte'` (พร้อมเลขรอบ) ออกมาแล้วรอ
  `resolveLastStandQte(state, opKey, hours)` ตอบกลับว่าได้กี่ชั่วโมง (0 = ล้มตามกฎเดิม)
  นับรอบที่เล่นไปแล้วด้วย `state.lastStandQteDone` — ผ่านแล้วตั้ง `lastStandQte` กลับเป็น `null`
  เพื่อให้เปิดได้อีกตอนเวลาที่ต่อมาหมด · ครบ `rounds.length` แล้วถึงจะเป็น `'done'` ถาวร
- **กันสถานะค้าง**: ถ้าไม่มีใครเปิดมินิเกมให้ (เช่นโค้ดจำลองที่ไม่มีหน้าจอ) เกินกี่ลูปตาม
  `qte.pendingGraceLoops` จะถือว่าอดต่อเวลาแล้วล้มเอง ไม่ปล่อยให้ Robertson ค้างในสถานะฟรีทั้งเกม

### เพลงตอน Last Stand

**ถอด `Stand Alone.mp3` ออกแล้ว** (ลบไฟล์ด้วย — ถ้าอยากได้คืนอยู่ใน git history)
ตอน Last Stand ยึด `London Bridge.mp3` ไว้เสมอ ไม่สลับไปเพลงเร่งแม้จะอยู่ช่วง 12 ชม.ท้าย

พอเข้าสถานะ เกม **กรอเพลงถอยหลัง** ไปตั้งต้นที่ `เป้า − (เวลาจริงของ 6 ชม.ในเกม)`
เพลงจึงไหลมาถึง **ช่วง 2:57–3:00 พอดีตอน QTE รอบแรกเปิด** โดยไม่ต้องตัดกลางเพลง
เป้าที่เล็งคือ **2:58.5 (`bgmCueSec`) กลางช่วงพอดี** เผื่อคลาดได้ทั้งสองทางแล้วยังอยู่ในช่วง
ตอน QTE เปิดจริงเช็คซ้ำ คลาดเกิน `bgmCueWindowSec` (1.5 วิ) ค่อยกรอแก้ — ผู้เล่นเปลี่ยนความเร็ว/หยุดเวลาได้ระหว่างนั้น
**เช็คตรงนี้เฉพาะรอบแรกเท่านั้น** — รอบ 2/3 มาทีหลังอีกหลายชั่วโมงในเกม ถ้าไปกรอด้วยเพลงจะกระโดดถอยหลัง
จบมินิเกมแล้วเพลงเล่นต่อจากตรงนั้นเลย ไม่เริ่มใหม่

> ⚠️ **การกรอเพลงต้องให้ที่ฝากไฟล์รองรับ Range request** — GitHub Pages รองรับ
> แต่ `python3 -m http.server` ไม่รองรับ เบราว์เซอร์จะดีดกลับไปวินาที 0 แล้วเทสจะฟ้องผิด
> เวลาเทสเรื่องเพลงต้องใช้ `scratchpad/serve.py` แทน (`python3 serve.py 8123`)

**ผลต่อบาลานซ์**: จำลอง 200 เกม/กลยุทธ์ โดยให้ "ไม่ได้ต่อเวลาเลย" (เป็นพื้นล่างสุด)
ได้ 505 / 538 / 547 / 489 / 530 คน — แทบไม่ต่างจากก่อนแก้ (505/541/539/487/527)
เพราะ Last Stand โผล่แค่ 7-39% ของเกม **QTE จึงเป็นกำไรล้วนสำหรับคนเล่นเก่ง ไม่ได้ตัดของเดิมทิ้ง**

**เทสอยู่ที่ `scratchpad/qte.js` (23 เคส — เล่นเองครบทั้ง 3 รอบแยกกัน · รอบ 2 เล่นด้วยคีย์ Q/E · และทางแพ้)
กับ `scratchpad/qtedev.js`**
(ปุ่มกดโดนจริงบนจอ 5 ขนาด — เล็กสุด 41px)

## ปรับบาลานซ์ Robertson (รอบ 10.12) — เจ้าของเคาะทุกข้อ

**Crowd Control**

| | เดิม | ใหม่ |
|---|---|---|
| ราคา | 14 AP | **10 AP** |
| ลงโซนไหนได้ | โซนไหนก็ได้ | **เฉพาะโซนที่มี จนท. ลงพื้นที่อยู่** (`needsUnit` ใน config) |

เหตุผล: มันคือบัฟ "ช่วยคนที่กำลังลงพื้นที่" ไม่ใช่บัฟดักไว้ล่วงหน้า
ผลข้างเคียงที่ตั้งใจ: Robertson ลงโซนเองแล้วจะกดไม่ได้ (ตัวเขาไม่ว่าง) มันจึงกลายเป็นบัฟไว้ **หนุน Lyla**

**Last Stand** — ตัวเลขทั้งหมดอยู่ที่ `CONFIG.lastStand`

| | เดิม | ใหม่ |
|---|---|---|
| เวลาทำงาน เทา/เหลือง/แดง | 1 / 2 / 3 ชม. | **0.5 / 1 / 1.5 ชม.** (เร็วขึ้นเท่าตัว คนที่ช่วยได้เท่าเดิม) |
| โอกาสสำเร็จ (ทอยชั้น 1) | ไม่มีบัฟ | **+10** (`rateBonus`) |
| %ช่วยชีวิต (ทอยชั้น 2) | ไม่มีบัฟ | **+10%** เพดาน 100 (`rescuePctBonus`) |
| ป้ายราคาบนการ์ด | โชว์ −6/−12/−20 ค้างไว้ | **FREE** (เกมหักแต้ม 0 อยู่แล้ว ที่ผิดคือหน้าจอ) |
| ตอนเข้าสถานะ | ไม่มีอะไรบอก | **ป้าย LAST STAND กลางจอ ลอยขึ้นแล้วจางหายเอง** (`showLastStand()` ใน `ui/fx.js`) |
| เวลาที่มี | 10 ชม. รวด | **6 ชม. แล้วต่อได้อีกสูงสุด 6 ชม. ด้วย QTE** (รอบ 10.13) |

ผลจำลอง 200 เกม/กลยุทธ์ (เทียบก่อน→หลัง) — ขึ้นราว 1-5% ไม่ได้พังบาลานซ์:
เทาก่อน 505→505 · โลภ 528→**541** · เหลืองก่อน 531→**539** · ทุ่มแดง 463→**487** · แดงก่อน 523→527
Last Stand โผล่ 8-38% ของเกม (ใช้ได้ครั้งเดียวต่อเกม และต้องล้มทั้งคู่ก่อน จึงไม่กลายเป็นของฟรี)

**เทสอยู่ที่ `scratchpad/laststand.js`** (17 เคส) — แตะอะไรใน Last Stand หรือ Crowd Control ต้องรันซ้ำ

## แก้เกมหนืด (รอบ 10.11) — ผู้เล่น iOS บอกว่า "แล็คมาก"

วัดด้วย `scratchpad/perf.js` (หน่วง CPU 6 เท่าให้เท่ามือถือ) ไม่ได้เดาเอา
เจอ 5 จุด ทุกจุดคือ "ทำงานทิ้งเปล่า" ไม่ใช่เกมหนักเกินไป — **กฎเกมและหน้าตาไม่เปลี่ยนเลยสักอย่าง**

| # | สาเหตุ | แก้ยังไง |
|---|---|---|
| 1 | **วาดแถบ จนท. ใหม่ 60 ครั้ง/วิ ทั้งที่เลขเท่าเดิม** — สั่งเขียนค่าเดิมซ้ำก็ยังทำให้เบราว์เซอร์คำนวณผังหน้าใหม่ทั้งหน้า | เขียนเฉพาะตอนค่าเปลี่ยนจริง + ลดเหลือ 30 ครั้ง/วิ |
| 2 | **`filter: brightness()` เปิดค้างตลอดเวลา** — ค่ากลางของแถบความสว่างคือ 0.95 ไม่ใช่ 1 เกมเลยติด filter ทั้งเวทีทุกเฟรม ทั้งที่ภาพไม่ต่างเลย | ค่ากลาง = 1.00 พอดี · ใส่ filter เฉพาะตอนผู้เล่นเลื่อนแถบจริง (`html.is-dimmed`) |
| 3 | **เส้นสแกน + ลายวิ่งบนแถบ% ขยับด้วย `background-position`** = สั่งระบายสีใหม่ทั้งจอทุกเฟรม | ขยับด้วย `transform` แทน (ภาพเหมือนเดิมเป๊ะ การ์ดจอทำให้เอง ไม่ต้องระบายใหม่) |
| 4 | **ลากไอคอนแล้วหนืด** — ไอคอนผีขยับด้วย `left/top` + ถาม "ใต้นิ้วคือโซนไหน" ทุกครั้งที่นิ้วขยับ (มือถือยิง 120 ครั้ง/วิ) | ขยับด้วย `transform` · รวบหลายครั้งให้เหลือรอบละ 1 ครั้งต่อการวาดจอ 1 เฟรม |
| 5 | **โหลดเพลง 13 MB ตั้งแต่เปิดเกม** ทั้งที่ยังอยู่หน้าปก เน็ตมือถือตันจนทุกอย่างมาช้า | `lazy: true` ใน `data/sounds.js` → เริ่มโหลด 2 วิหลังแตะหน้าปก (ยังเหลือเวลาอีกเป็นนาทีกว่าจะถึงหน้าเกม) |

**ผลที่วัดได้** (หน่วง CPU 6 เท่า · จอ 873×337 · เล่นจริง 12 วินาที):

| | ก่อน | หลัง |
|---|---|---|
| คำนวณผังหน้าใหม่ | 706 ครั้ง · 1,252 ms | **77 ครั้ง · 122 ms** |
| เวลาทำงานรวมของ CPU | 7,640 ms | **3,854 ms** |
| เฟรมหลุด (ต่ำกว่า 30fps) | 21 เฟรม (3.0%) | **2 เฟรม (0.3%)** |
| ตอนลากไอคอน 300 ก้าว | ผัง 383 ครั้ง · รวม 8,659 ms | **ผัง 81 ครั้ง · รวม 6,423 ms** |
| โหลดกว่าจะถึงหน้าเมนู | 13.5 MB | **1.0 MB** |

**รูปที่ใหญ่เกินจำเป็นก็ย่อด้วย** — ขนาดไฟล์ไม่ใช่ประเด็น แต่ตอนแสดงผลรูปถูกคลี่เป็นบิตแมปเต็มขนาด
ซึ่งกินแรม (iOS ยิ่งอ่อนไหวเรื่องนี้) `Briefroom.JPG` 3086px → 2048px · ไอคอนสกิลใหญ่ 2 อัน 1024px → 512px
รวมประหยัดแรมตอนแสดงผล **22 MB** และไฟล์ที่ต้องโหลด 0.9 MB — ตาเปล่าดูไม่ออกเพราะของเดิมใหญ่เกินที่จอโชว์ได้อยู่แล้ว
**รูปตัวละครไม่ย่อ** เพราะหน้าสอนเล่นโชว์ตัวใหญ่เต็มจอ (ย่อแล้วจะเบลอ)

**⚠️ กฎที่ต้องจำ เวลาเขียนโค้ดต่อ:**
- อะไรที่ทำงานทุกเฟรม (`requestAnimationFrame` · `pointermove` · `animation: infinite`)
  **ห้ามแตะ `left/top/width/background-position`** ใช้ `transform`/`opacity` เท่านั้น
  สองอันหลังการ์ดจอขยับให้เองโดยไม่ต้องระบายภาพใหม่ ที่เหลือคือสั่งวาดใหม่ทั้งจอ
- **ห้ามเขียนค่าลง DOM ทั้งที่ค่าเดิม** — เบราว์เซอร์ไม่เช็คให้ ต้องเช็คเอง (จำค่าที่เขียนไปล่าสุดไว้)
- **ห้ามใส่ `filter` ค้างไว้บนของชิ้นใหญ่** ต่อให้เป็นค่าที่ไม่เปลี่ยนภาพเลยก็ยังเสียเวลาเท่าเดิม
- **เทสอยู่ที่ `scratchpad/perf.js` และ `scratchpad/dragperf.js`** — แตะอะไรที่วาดถี่ ๆ ต้องรันซ้ำ

## ล็อกอัตราส่วนจอ (รอบ 10.10) — "เวที" 1320×600

เจ้าของเล่นบนมือถือจริงแล้วเจอ **ปุ่มหลุดขอบ · สัดส่วนเละ · ภาพหาย** สาเหตุมี 3 ชั้น:

1. **ไม่เคยเผื่อขอบบาก (notch)** — `env(safe-area-inset-*)` ไม่ถูกใช้เลยสักที่ ตอนถือแนวนอนบากกินซ้าย/ขวาข้างละ ~47px เนื้อหาเลยมุดไปอยู่ใต้บาก
2. **ทุกขนาดผูกกับ `vh`/`vw`** ซึ่งอิงหน้าต่างจริง — บนมือถือแถบ URL ยืด/หดตลอด เลย์เอาต์เลยขยับตามไม่หยุด
3. **จอเตี้ยกว่า ~483px ใส่ไม่ลงจริง ๆ** แล้วโดน `overflow: hidden` ตัดทิ้งเงียบ ๆ (จอ 20:9 เห็นสูงจริงแค่ ~306px)

**วิธีแก้: วาดเกมลง "เวที" ขนาดคงที่ 1320×600 เสมอ** แล้วย่อ/ขยายทั้งเวทีด้วย `transform: scale()`
ให้พอดีพื้นที่ที่ใช้ได้จริง ที่เหลือเป็นแถบดำ (letterbox) เหมือนดูหนัง → **ทุกเครื่องเห็นเหมือนกันเป๊ะ**
โค้ดอยู่ที่ `src/ui/stage.js` เรียก `initStage()` เป็นบรรทัดแรกของ `main.js`

- วัดพื้นที่จาก `visualViewport` (พื้นที่ที่เห็นจริงหลังหักแถบ URL) ไม่ใช่ `innerHeight`
- อ่านขนาดบากผ่าน element ตัววัดที่ตั้ง `padding: env(safe-area-inset-*)` เพราะ JS อ่าน `env()` ตรง ๆ ไม่ได้
- คำนวณใหม่ทุกครั้งที่ resize / หมุนจอ / แถบ URL ยืดหด

> ⚠️ **ห้ามใช้หน่วย `vh`/`vw` ใน CSS ที่อยู่ข้างในเวที** — มันอิงหน้าต่างจริง ไม่ใช่เวที
> ใส่แล้วสัดส่วนจะหลุดกลับไปเพี้ยนเหมือนเดิม ใช้ `px` ตรง ๆ ได้เลยเพราะเวทีสูง 600 คงที่
> (ยกเว้นหน้า "หมุนจอ" `.rotate-*` ที่อยู่นอกเวที ใช้ vh/vw ถูกแล้ว)
> `@media (max-height: ...)` ก็ห้ามใช้กับของในเวทีด้วยเหตุผลเดียวกัน — ลบออกหมดแล้ว

### รื้อการ์ด จนท. — เพราะนิ้วกดไอคอนสกิลไม่โดน

ล็อกอัตราส่วนอย่างเดียวยังไม่พอ **ไอคอนสกิลเหลือ 18px บนมือถือ กดไม่โดน**
สูตรที่ต้องจำ: **ขนาดที่เห็นจริง = (ขนาดปุ่มเป็นหน่วย u ÷ ความสูงเวทีเป็นหน่วย u) × ความสูงจอจริง**
→ ขยายเวทีไม่ช่วยเลย ต้องทำให้ **ดีไซน์ "เตี้ยลงเป็นหน่วย u"** อย่างเดียว

ที่ทำ: **การ์ด จนท. เปลี่ยนจากแนวตั้งเป็นแนวนอน** (รูปอยู่ข้าง ๆ ไม่ใช่ข้างบน) ·
ป้ายสถานะไปทับบนรูปแทนที่จะกินความสูง · ป้ายประเภทสกิล 📥/✨ ไปเกาะมุมไอคอนแทนที่จะเว้นที่ข้างบน

| | เดิม | ใหม่ |
|---|---|---|
| ความสูงดีไซน์ | 70 หน่วย (`--u` 8.6px) | **57 หน่วย** (`--u` 10.5px) |
| การ์ด จนท. 1 ใบ | 25.7 หน่วย | **16.8 หน่วย** |
| ไอคอนสกิล | 4.2 หน่วย = 6% ของจอ | **6 หน่วย = 10.5% ของจอ** |
| ไอคอนบนมือถือเล็กสุด | 18px | **32px** |

**เทสอยู่ที่ `scratchpad/devices.js`** — 9 รุ่นจริงตั้งแต่ 6.0"–7.0" แท็บเล็ต และคอม
จำลองทั้งแถบ URL กินความสูงและขอบบากกินด้านข้าง เช็คว่าเวทีอยู่ในจอครบ · อัตราส่วนคงที่ 2.200 ทุกเครื่อง ·
คอลัมน์ไม่ล้น · **ไอคอนสกิลทั้ง 6 อันกดโดนจริงและใหญ่ ≥24px**
**แตะอะไรในแถบ จนท. หรือขนาด `--u` ต้องรัน `devices.js` + `layout.js` ซ้ำเสมอ**

## การปรับบาลานซ์รอบ 8.5 — เคาะกับเจ้าของแล้ว

เดิมเล่นยังไงก็ช่วยได้ 12% ของเมืองเท่ากันหมด ฝีมือไม่มีผลเลย สาเหตุมี 2 ชั้น:
1. **แต้มไม่พอ** — เริ่ม 0 · 5/ชม. + โบนัสที่ผูกกับ "จำนวนคนตาย" (มาช้าเสมอ)
   แต้มสูงสุดที่สะสมได้ทั้งเกม = 25 แต่โซนแดงราคา 30 → ลงโซนแดงไม่ได้เลยสักครั้ง
2. **โซนแดงเป็นดีลที่ขาดทุน** — โทษพื้นที่ −55 ทำให้ Lyla สำเร็จแค่ 35%
   ได้คนต่อชั่วโมงน้อยกว่าโซนเหลือง ทั้งที่โซนแดงมีคน 45% ของเมือง
   **แก้แต้มอย่างเดียวไม่ขยับเลย ต้องแก้ทั้งสองชั้นพร้อมกัน**

สิ่งที่เปลี่ยน (ตัวเลขจริงอยู่ใน `src/config.js` ทั้งหมด):

| | เดิม | ใหม่ |
|---|---|---|
| AP เริ่มเกม | 0 | **80** (`apStart`) |
| AP ต่อชั่วโมง | 5 + โบนัสคนตาย | **25 / 15 / 8** ตามช่วงเวลา (`apCurve`) |
| โทษพื้นที่ เหลือง · แดง | −30 · −55 | **−25 · −40** |
| อายุโซนแดง | 24 ชม. | **32 ชม.** |
| Robertson เข้าโซนแดง | ไม่ได้ | **ได้ 20 AP · 3 ชม.** (ฐาน 35% ไม่บัฟคือไปตาย) |
| Air Deploy | 24 AP · 1 ชม. | **20 AP · 3 ชม. · ชะลอการตายทั้งแมพ 40%** |
| Alert Allied | 35 AP · คูลดาวน์ 4 ชม. | **15 AP · คูลดาวน์ 3 ชม.** |
| อันตรายฐาน เทา/เหลือง/แดง | 5/15/30% เพดาน 50% | **12/30/48% เพดาน 72%** |
| บาดเจ็บฟื้น · หมดสติ | 3 · 4 ชม. | **6 · 9 ชม.** |

ผลที่วัดได้ (200 เกม/กลยุทธ์): ช่วยได้ **27% (เล่นปลอดภัย) → 37% (เล่นบู๊)** ·
Last Stand โผล่ 6–37% ของเกม · Air/Alert ถูกใช้จริงแล้ว (เดิม 0 ครั้ง)

**ตอนจบเกม คนที่ยังติดอยู่นับเป็นเสียชีวิตทั้งหมด** — บัฟชะลอการตายยืดชีวิตโซนเกินอายุปกติได้
เกมจึงจบลงตอนที่ยังมีคนไม่ตายค้างอยู่ กฎนี้ทำให้ **ช่วยได้ + เสียชีวิต = 1,200 เป๊ะเสมอ**
อยู่ที่ `endGame()` ใน `systems/time.js` ซึ่งเป็นทางเดียวที่ `state.ended` จะเป็น true

## การปรับบาลานซ์รอบ 10.5 — หลังเจ้าของลองเล่นจริง

เจ้าของรายงาน 3 อย่าง วัดแล้วจริงทั้งหมด:

**1. คนกระจุกที่โซนแดง โซนเทา/เหลืองถูกทิ้ง**
เดิม เทา 22% · เหลือง 34% · **แดง 44%** ของทั้งเมือง (โซนแดง 8 โซนถือคนเกือบครึ่ง)
วัด "คนที่ช่วยได้ต่อชั่วโมง" ได้ เทา 6.5 · เหลือง 15.8 · แดง 12.8 → โซนเทาไม่มีเหตุผลให้ไปเลย
→ เกลี่ยเป็น **36/30/33%** (เทาโซนละ 14-18 คน) ต่อชั่วโมงกลายเป็น 10.4 / 14.2 / 9.8 สูสีกันแล้ว

**2. ชั่วโมง 50 ไม่มีอะไรให้ทำ**
เดิมโซนแดงหมดอายุ 32 ชม. เหลือง 48 ชม. → ชั่วโมง 50 ทั้งเมืองเหลือคนแค่ **82 คน**
หนึ่งในสามท้ายของเกมว่างเปล่า
→ ยืดอายุเป็น **เทา 80 · เหลือง 66 · แดง 54 ชม.** ชั่วโมง 50 เหลือ **281 คน**
เทา 80 ชม. > เกม 72 ชม. โดยตั้งใจ โซนเทาจึงไม่มีวันตายเกลี้ยงก่อนหมดเวลา
จุดหยุดเวลาอัตโนมัติขยับเป็น **[30, 54]** ให้ตรงกับวันหมดอายุโซนแดงใหม่

**3. ตายเกินครึ่งแต่ได้แรงก์ A**
เดิม `rescueTarget = 0.45` → ช่วย 45% กับ 100% ได้คะแนน**เท่ากันเป๊ะ** (ตันทั้งคู่)
และช่วยแค่ 30% (ตาย 70%) ก็ได้ A แล้ว
→ ยกเป้าเป็น **0.60** (เหนือเพดานจริง ~55% เล็กน้อย ส่วนนี้จึงไม่ตันในทางปฏิบัติ)
→ ย้ายน้ำหนักไปที่ "จำนวนคนที่ช่วยได้" **900 / 150 / 150** (เดิม 780/240/180)
   เพราะส่วนโซน+ความปลอดภัยเป็นแต้มที่เล่นพอเป็นก็ได้ ถ้าให้น้ำหนักมากจะดันแรงก์ขึ้นฟรี
→ ยกเกณฑ์แรงก์ทั้งแถบเป็น **S 1090 · A 950 · B 780 · C 600 · D 380**

**🐛 บั๊กที่เจอเพิ่ม — "ช่วยฟรี"**
กล่องล่างนับ `zone.cleared` เป็น "Clear%" แต่โซนที่**คนตายเกลี้ยงเองก็ถูกมาร์ค cleared**
ผลคือชั่วโมง 48 ขึ้น "โซนเหลือง Clear 100%" ทั้งที่ไม่ได้ช่วยใครเลยสักคน
→ เปลี่ยนไปนับ `zone.rescued >= 1` เกณฑ์เดียวกับ `systems/score.js`

**ผลจำลอง 80 เกม/ฝีมือ** (ฝีมือมีผลจริงแล้ว ต่างกัน 2 เท่า):

| ฝีมือ | ช่วยได้% | คะแนนกลาง | แรงก์ |
|---|---|---|---|
| ไม่ลงมือเลย | 0% | 10 | E ล้วน |
| เล่นมั่ว | 33% | 589 | C (กระจาย E–B) |
| เลือกโซนเป็นแต่ไม่ใช้บัฟ | 41% | 740 | B |
| ใช้บัฟถูกวิธี | 49% | 955 | A (แตะ S 9%) |

**เพดานจริงของเกมคือช่วยได้ราว 55%** — เพราะทอย 2 ชั้น (อัตราสำเร็จ × %ช่วยชีวิต)
คูณกันแล้วดึงคนออกได้ราวครึ่งเดียวของโซนเสมอ **บัฟคือตัวถีบเพดานนี้** (บัฟครบดันสำเร็จถึง 99%)

## ระบบคะแนนรอบ 10.3 — เคาะกับเจ้าของแล้ว

ระบบเก่าคิด "ช่วยได้ − เสียชีวิต" ผลคือ **เล่นเก่งแค่ไหนก็ติดลบตลอด**
เพราะช่วยได้เต็มที่ราว 37% ของเมือง อีก 63% เป็นลบเสมอ ช่วงคะแนนจริงคือ −1,200 ถึง −300

ระบบใหม่วัด "ทำได้ดีแค่ไหนเทียบกับที่ทำได้จริง" → **คะแนน 0–1,200 พร้อมแรงก์ S–E**
ตัวเลขทั้งหมดอยู่ที่ `CONFIG.score` และ `CONFIG.ranks` · สูตรอยู่ที่ `systems/score.js`

| ส่วน | เต็ม | เกณฑ์ได้เต็ม |
|---|---|---|
| จำนวนคนที่ช่วยได้ | 900 | ช่วยได้ 60% ของเมือง |
| ความครอบคลุมพื้นที่ | 150 | ช่วยได้ 40 จาก 47 โซน |
| ความปลอดภัยของทีม | 150 | ไม่เจ็บเลย · ต้องส่งภารกิจครบ 8 ครั้งถึงปลดล็อกส่วนนี้ |

*(ตัวเลขข้างบนคือหลังปรับรอบ 10.5 แล้ว — เดิมคือ 780/240/180 เป้า 45%)*

- จบเพราะ CRITICAL → คูณ 0.6
- บีบให้อยู่ใน **[10, 1200]** เสมอ — ไม่ลงมือเลยได้ 10 (แรงก์ E) · สมบูรณ์แบบได้ 1,200 (แรงก์ S)
- **ปัดรายส่วนก่อนบวก** ไม่ใช่บวกก่อนปัด ไม่งั้นเลขบนจอบวกกันไม่ลงตัว
- ส่วนความปลอดภัยผูกกับ `state.stats.missions` เพราะถ้าไม่กันไว้ **นั่งเฉย ๆ จะได้ 180 ฟรี**

ผลจำลอง 150 เกม/กลยุทธ์: ไม่ลงมือ 10 (E ล้วน) · เล่นมั่ว 82–1,121 (กระจายทุกแรงก์) ·
เล่นปลอดภัย 181–1,128 (กลาง 965 = A · แตะ S 11%)

**⚠️ `data/operators.js` ไม่เก็บตัวเลขเองแล้ว** — ราคา AP · เวลาทำงาน · ค่าบัฟ อ่านจาก `CONFIG` ทั้งหมด
(เมื่อก่อนพิมพ์ซ้ำไว้ 2 ที่ ผลคือแก้ `config.js` แล้วเกมไม่เปลี่ยน เพราะโค้ดจริงอ่านจาก operators.js)

## ธีมหน้าตา (รอบ 10.6) — "หน้าจอสั่งการ"

**สีและฟอนต์ทั้งเกมมาจากตัวแปรชุดเดียวที่หัวไฟล์ `src/styles.css`** (`:root`)
อยากเปลี่ยนโทนทั้งเกมแก้ที่นั่นที่เดียว ไม่ต้องไล่แก้ทีละหน้า

- พื้นน้ำเงินเข้ม + ตารางกริดจาง + เส้นสแกนไล่ลง (`.screen-overlay::before/::after`)
- **ไม่ใช้เว็บฟอนต์จากอินเทอร์เน็ตโดยตั้งใจ** — ปลายทางเป็นไฟล์เดียวที่ต้องเปิดได้แม้ไม่มีเน็ต
  ใช้ฟอนต์ในเครื่องเรียงเป็นชั้น (`--font-display` คอนเดนส์ · `--font-mono` ตัวเลข · `--font-ui` ไทย)
  ถ้าอยากได้ฟอนต์เฉพาะจริง ๆ ต้องหาไฟล์ `.ttf` มาแล้วฝัง base64 ตอน build
- **ตึกบนหน้าเมนูวาดด้วยโค้ด** (`ui/skyline.js`) ไม่ใช่ไฟล์รูป — ไม่เพิ่มขนาดไฟล์ตอน build
  สุ่มแบบมีซีดคงที่ เปิดกี่ครั้งเมืองก็หน้าตาเดิม · สั่นเป็นระยะทุก 7 วิ (ชั้นใกล้สั่นแรงกว่าชั้นไกล)
- **แถบ % บนการ์ด จนท.** — ค่าคิดที่ `systems/skills.js` `unitReadiness()` ไม่ใช่ที่ ui/
  กำลังทำงาน = %ความคืบหน้าภารกิจ · บาดเจ็บ/หมดสติ = %เวลาที่ฟื้นไปแล้ว · ว่าง = 100%
- กล่องล่างมีแถบเล็ก ๆ ข้างตัวเลข ดูค่าคร่าว ๆ ได้โดยไม่ต้องอ่านเลข

**⚠️ กับดักที่เจอตอนทำ (เทส `layout.js` กันไว้แล้ว):**
แถบ % ทำให้การ์ด จนท. สูงขึ้นราว 50px ต่อใบ → คอลัมน์ล้น → **ไอคอนสกิลของ จนท. คนล่างถูกตัดจนลากไม่ได้**
ทั้งที่หน้าจอ "ดูปกติ" ทุกอย่าง เพราะ `.game-side` มี `overflow: hidden` มันเลยแค่ถูกซ่อน ไม่ได้ดันอะไรให้เห็น
**เพิ่มอะไรในแถบ จนท. ต้องรัน `layout.js` เสมอ** — มันเช็คว่าไอคอนสกิลทุกอัน "กดโดนจริง" ใน 4 ขนาดจอ

## เสียงในเกม (assets/*.mp3)

**ตารางจับคู่อยู่ที่ `src/data/sounds.js` ที่เดียว** — ห้ามพิมพ์ชื่อไฟล์เสียงซ้ำที่อื่น
⚠️ ชื่อไฟล์จริงสะกดไม่ตรงกับที่คุยไว้ (`success-` ไม่ใช่ `sucess-`) บางอันมี `!` บางอันมีเว้นวรรค
`soundPath()` ทำ `encodeURIComponent` ให้แล้ว ห้ามประกอบ path เอง

### เพลงประจำฉาก (bgm) — เจ้าของสถานะมีคนเดียวคือ `ui/audio.js`

| ฉาก | เพลง | หมายเหตุ |
|---|---|---|
| เมนู · Settings · Intel · การ์ด จนท. · หน้า Result | `result.mp3` | วนซ้ำ เดินข้ามหน้าไม่เริ่มใหม่ |
| หน้าเกม ชั่วโมง 0–59 | `London Bridge.mp3` (7:45) | วนซ้ำ |
| หน้าเกม 12 ชั่วโมงท้าย | `1minuteleft!.mp3` (51 วิ) | วนซ้ำจนหมดเกม (`CONFIG.bgmFinalHours`) |
| ช่วง Last Stand ของ Robertson | ยังเป็น `London Bridge.mp3` | **กรอเพลงให้ถึง 2:57–3:00 พอดีตอน QTE เปิด** |
| หน้าปก · หน้าบรีฟ | เงียบ | กด Play = เพลงเมนูหยุดทันที |

**ตอนลากไอคอน เวลาหยุดแต่เพลงเล่นต่อ** (เจ้าของสั่ง) — การลากคือช่วงที่ผู้เล่นกำลังคิดอยู่ในเกม
ต่างจากการกดปุ่มหยุด / เปิดเมนู ☰ / เข้า Settings ซึ่งหยุดเพลงด้วยทั้งหมด
ทำด้วยธง `bgmKeepPlaying` ใน `ui/gameScreen.js` ที่ยกขึ้นเฉพาะ `dragCtx.pause`
และ **ยกขึ้นเฉพาะตอนที่ก่อนลากเวลาเดินอยู่** — ถ้าผู้เล่นกดหยุดไว้ก่อนแล้วค่อยลาก เพลงต้องเงียบอยู่เหมือนเดิม
(เทสอยู่ที่ `scratchpad/bgmdrag.js`)

**⚠️ ห้ามให้หน้าไหนสั่ง play/pause กับ `<audio>` เอง** — เดิมทำแบบนั้นแล้วมีจุดที่ลืมสั่ง
เพลงเลยเล่นต่อทั้งที่กดหยุดเกมไปแล้ว ตอนนี้เหลือทางเดียวคือ `setBgm()` / `setBgmPaused()`
และหน้าเกมเรียก `syncGameBgm()` จากที่เดียวกับที่เดินเวลา (ทุกลูป + ทุกจุดที่หยุด/เดินต่อ)

### เสียงสั้น (sfx)

| ไฟล์ | ใช้ตอนไหน |
|---|---|
| `click.mp3` | **ทุกการกดปุ่ม/ลากไอคอน** — ดักรวมศูนย์ที่ `document` ด้วย `pointerdown` |
| `success-notification.mp3` | ช่วยสำเร็จ · จนท. หายบาดเจ็บ/หมดสติ |
| `op-down.mp3` | ช่วยไม่สำเร็จ · จนท. บาดเจ็บ/หมดสติ |
| `point-counter.mp3` | ตอนเลขคะแนนวิ่งในหน้า Result (เล่นทับเพลงได้ = ชนิด `cue`) |

"ไปไม่ทัน" (`toolate`) **เงียบ** — เจ้าของไม่ได้สั่งเสียงไว้

### ระดับเสียง — วัดมาจากไฟล์จริง ไม่ได้เดา

วัด RMS ของช่วงที่ดังที่สุด (หน้าต่าง 150 มิลลิวินาที) ของทุกไฟล์ พบว่าต่างกันถึง **24 dB**
(`result` 0.641 · `op-down` 0.588 · `London Bridge` 0.405 · **`click` 0.042**)

`el.volume` ตั้งได้แค่ 0..1 คือ "ลดอย่างเดียว" จึงดัน `click` ให้ดังเท่าคนอื่นไม่ได้
**ระบบเสียงจึงเดินผ่าน Web Audio** ที่ตั้ง gain เกิน 1 ได้ พร้อม limiter กันเสียงแตก:

```
<audio> → MediaElementSource → gain ประจำเสียง → gain รวม (Settings) → limiter → ลำโพง
```

ตั้งเป้าให้ **เสียงเอฟเฟกต์ 0.22 · เพลง 0.12** (เพลงเบากว่าโดยตั้งใจ ไม่ให้กลบเสียงเอฟเฟกต์)
`gain` คำนวณจากค่าที่วัดได้ใน `sounds.js` เปลี่ยนไฟล์เสียงเมื่อไหร่ต้องวัดใหม่แล้วแก้เลขที่นั่น

- เสียงสั้นมีหลายก๊อปปี้ต่อ 1 ไฟล์ (`pool`) ไม่งั้นกดรัว ๆ แล้วเสียงตัดตัวเอง
- เบราว์เซอร์บล็อกเสียงจนกว่าผู้เล่นแตะจอครั้งแรก — ปลดล็อกตอนแตะหน้าปก
- `<audio>` ถูกใส่ลง DOM จริง (ไม่มี controls) เพื่อให้เทส/DevTools ตรวจได้ว่าอะไรกำลังเล่น

## หน้า Settings — ใช้ได้จริงแล้ว

ค่าอยู่ที่ `src/data/prefs.js` จำไว้ใน `localStorage` เปิดเกมใหม่ยังได้ค่าเดิม

- **Volume** → คูณเข้า gain รวมของ Web Audio · ยกกำลัง 2 เพราะหูคนไม่ได้ยินเป็นเส้นตรง
- **Brightness** → `filter: brightness()` ที่ `#screen-root` และชั้นเอฟเฟกต์
  (ไม่ใส่ที่ `body` เพราะ filter จะทำให้ `position: fixed` ข้างในเปลี่ยนจุดอ้างอิง)

## ไอคอนในเกม (assets/icons/)

**อัปได้ทั้งโฟลเดอร์ ไม่ต้องเปลี่ยนชื่อไฟล์** — ตารางจับคู่ "ชื่อไฟล์ ↔ ที่ใช้ในเกม" อยู่ที่ `src/data/icons.js` ที่เดียว
ทุกอันมี emoji สำรอง ไฟล์ไหนยังไม่มีก็ใช้ emoji เดิมไปก่อน เกมไม่พัง
`probeIcons()` ใน `main.js` ตรวจตอนเปิดเกมว่าไฟล์ไหนโหลดได้จริง

| ที่ใช้ในเกม | ไฟล์ | emoji สำรอง |
|---|---|---|
| ปุ่มหยุด/เดินเวลา | `pause-solid.svg` · `play-solid.svg` | ⏸ ▶ |
| ปุ่มความเร็ว | `number1-square-solid.svg` · `number2-square-solid.svg` | 1️⃣ 2️⃣ |
| บัฟ Crowd / Scan / Alert / Air | `crowd-control.svg` · `eye-solid.svg` · `alliedalert.svg` · `airdeploy.svg` | 👥 👁 ⚠ 🚁 |
| ประเภทสกิล ลงพื้นที่ / บัฟ | `field-deploy.svg` · `sparks-solid.svg` | 📥 ✨ |
| สถานะ หมดสติ / Last Stand / ฟื้น | `unconscious.svg` · `finalstand.svg` · `heart-solid.svg` | 💀 ⚡ 💚 |
| ปุ่ม Home / เล่นใหม่ | `home.svg` · `restart.svg` | 🏠 🔄 |
| สถานะ บาดเจ็บ | `warning-square-solid.svg` | 🩹 |
| ตัวละครหน้าปก | `human.png` · `beastfolk.png` · `elf.png` · `mytics.png` | 🥽 🐱 🧝 👻 |

**คงเป็น emoji ตามที่เจ้าของสั่ง:** วงกลมในการ์ด Feed 🟢 🔴 ⚫ — มีไฟล์แค่ฝั่งสำเร็จ (`check-circle-solid.svg`)
ถ้าเปลี่ยนแค่อันเดียวจะกลายเป็นสามอันคนละแบบ อ่านยากกว่าเดิม

**รูปตัวละคร 4 ตัวบนหน้าปก** โชว์เต็มรูป ไม่มีวงแหวนสีทับ (รูปมีขอบมาในตัวแล้ว) เว้นระยะเท่ากันทุกช่อง
ไฟล์ที่ไม่ได้ใช้: `check-circle.svg` `heart.svg` `flash-solid.svg` (เป็นเวอร์ชันเส้น เก็บไว้เฉย ๆ ได้)

**ปล่อยเป็นตัวอักษรตามเดิม ไม่ต้องทำรูป:** ✕ กากบาท · ☰ เมนู · + − ⌖ ปุ่มซูม · Ⓐ Ⓑ Ⓒ ป้ายระดับโซน

## แผนผังไฟล์

```
index.html            หน้า dev (โหลด src/ เป็น module)
src/
  config.js           ✅ ตัวเลขทั้งหมด
  state.js            ✅ สถานะเกม
  main.js             ✅ จุดเริ่มต้น
  styles.css          ✅
  data/operators.js   ✅ จนท. 4 ตัว + สกิล
  data/icons.js       ✅ จับคู่ไอคอนกับไฟล์ใน assets/icons/ + emoji สำรอง
  systems/            ตรรกะเกม — ห้ามแตะ DOM
    zones.js  time.js  actionPoints.js     ✅ เขียนแล้ว
    survivors.js death.js                  ⬜ stub
    skills.js                              ✅ สถานะ/เงื่อนไขใช้สกิล
    status.js                              ✅ บาดเจ็บ/หมดสติ/LastStand/CRITICAL
    successRate.js danger.js               ✅ สูตรอัตราสำเร็จ/อันตราย
    outcomes.js                            ✅ ทอยผลภารกิจ 2 ชั้น
    score.js                               ✅ คะแนน 0-1200 + แรงก์ S-E + สรุปผลปฏิบัติการ
  ui/                 แสดงผล/อินพุต — ห้ามคำนวณกฎเกม
    map.js                                 ✅ map.svg จริง + ซูม/ปัด
    gameScreen.js                          ✅ หน้าเกมหลัก
    tutorial.js                            ✅ VN สอนเล่น
    dragdrop.js                            ✅ ลากไอคอนสกิลลงโซน
    panels.js                              ✅ แผงข้อมูลโซนตอนลาก
    feed.js                                ✅ Detail Feed ผลภารกิจ
    qte.js                                 ✅ มินิเกมจังหวะต่อเวลา Last Stand
    gameMenu.js                            ✅ เมนู ☰ · หน้าเวลา · ป๊อปอัพหยุดเวลา
    hud.js                                 ⬜ stub
    screens.js                             ✅ หน้าจอนอกเกม
    result.js                              ✅ หน้า Result + แรงก์ + อนิเมชั่นนับคะแนน
    fx.js                                  ✅ ม่านดำเปลี่ยนหน้า · จอกระพริบ · กรอบเตือน
    skyline.js                             ✅ ฉากเมืองเวกเตอร์บนหน้าเมนู + อาฟเตอร์ช็อก
    audio.js                               ✅ ระบบเสียง — ที่เดียวที่แตะ <audio>
    stage.js                               ✅ ล็อกอัตราส่วนเวที 1320×600 + เผื่อขอบบาก
  utils/rng.js        ✅ ตัวสุ่มกลาง ตั้งซีดได้ตอนเทส
assets/               รูปตัวละคร/ไอคอนสกิล/พื้นหลัง (มาแล้วส่วนใหญ่)
map.svg               แผนที่ 47 โซนจริง
build/build.js        ⬜ stub — รวมเป็นไฟล์เดียว + Base64
dist/index.html       ไฟล์ส่งงานสุดท้าย
docs/                 สเปกทั้งหมด
```
