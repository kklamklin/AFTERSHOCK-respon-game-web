// ตัวเลขทั้งหมดของเกม — อ้างอิงจาก docs/gamesystemfinal.md §16.1
export const CONFIG = {
  totalLoops: 144,
  loopsPerHour: 2,
  totalSurvivors: 1200,
  maxSuccessRate: 99,

  // การกระจายคนต่อโซน (สุ่มในช่วงนี้ ปรับให้รวม 1200 พอดี)
  //
  // เดิม เทา 22% · เหลือง 34% · แดง 44% ของทั้งเมือง — โซนแดง 8 โซนถือคนเกือบครึ่ง
  // ผลคือทุกคนแห่ไปโซนแดง แล้วโซนเทา 26 โซน (โซนละ 9-11 คน) ถูกทิ้งจนตายเกลี้ยง
  // วัดจริงได้ว่า "คนที่ช่วยได้ต่อชั่วโมง" ของเทา 6.5 · เหลือง 15.8 · แดง 12.8 — เทาไม่มีเหตุผลให้ไป
  // ตอนนี้เกลี่ยเป็น 36/30/33% → ต่อชั่วโมงกลายเป็น 10.4 / 14.2 / 9.8 สูสีกันทั้งสามระดับ
  survivorsPerZone: {
    gray:   { min: 14, max: 18 },
    yellow: { min: 24, max: 32 },
    red:    { min: 46, max: 58 },
  },

  // จำนวนโซนต่อระดับ (จาก map.svg §3.1)
  // โซนเทา 26 (เดิมนับ 27 แต่ map.svg เขียน zone-gray-03 ซ้ำกับ zone-gray-12 — ลบตัวซ้ำแล้ว)
  zoneCounts: { gray: 26, yellow: 13, red: 8 },

  // อายุโซน (ลูปที่คนตายหมดถ้าไม่มีใครช่วย) — เทา 80 ชม. · เหลือง 66 ชม. · แดง 54 ชม.
  //
  // เดิม แดง 32 ชม. · เหลือง 48 ชม. → พอถึงชั่วโมง 48 ทั้งเมืองเหลือแต่โซนเทาที่มีคนละ 2-3 คน
  // หนึ่งในสามท้ายของเกม (24 ชม.) จึงไม่มีอะไรให้ทำเลย วัดได้ว่าเหลือคนแค่ 82 คนตอนชั่วโมง 50
  // ยืดออกให้ครึ่งหลังยังมีคนให้ช่วยจริง (เหลือ 281 คนตอนชั่วโมง 50) แต่คงลำดับความเร่งด่วนไว้
  // เทา 80 ชม. > เกม 72 ชม. โดยตั้งใจ — โซนเทาจึงไม่มีวันตายเกลี้ยงก่อนหมดเวลา
  zoneLifespanLoops: { gray: 160, yellow: 132, red: 108 },

  // ความเสี่ยงพื้นที่ (ตัวลบอัตราสำเร็จ)
  // เดิม เหลือง −30 / แดง −55 ทำให้โซนแดงเป็นดีลที่ขาดทุนเสมอ (Lyla สำเร็จแค่ 35%)
  // ไม่มีใครมีเหตุผลจะเข้าโซนแดงเลย ทั้งที่ในนั้นมีคน 45% ของเมือง
  zonePenalty: { gray: -10, yellow: -25, red: -40 },

  // โอกาสอันตรายพื้นฐาน (ก่อนคูณเวลา)
  // ทอยเฉพาะตอนภารกิจล้มเหลว บัฟจึงช่วย 2 ต่อ (สำเร็จมากขึ้น = โดนทอยอันตรายน้อยลง)
  // เลขฐานเลยต้องสูงกว่าที่รู้สึก ไม่งั้นบาดเจ็บแทบไม่เกิดเลยทั้งเกม
  dangerBase: { gray: 0.12, yellow: 0.30, red: 0.48 },
  dangerCap: 0.72,

  // สีโซนตาม §3.2
  zoneColors: {
    gray:   { fill: '#D9D9D9', stroke: '#000000' },
    yellow: { fill: '#FDF3D0', stroke: '#E8C13F' },
    red:    { fill: '#FBE4E4', stroke: '#E5484D' },
    green:  { fill: '#D4F4DD', stroke: '#3DD68C' },
  },

  // ── AP ────────────────────────────────────────────────────────
  // เดิม: เริ่ม 0 · 5/ชม. + โบนัสตามสัดส่วน "ผู้รอดที่หายไปแล้ว"
  // โบนัสนั้นผูกกับจำนวนคนตาย แต้มก้อนใหญ่จึงมาถึงตอนไม่มีใครให้ช่วยแล้ว
  // วัดจริงได้ว่าแต้มสูงสุดที่สะสมได้ทั้งเกมคือ 25 ทั้งที่โซนแดงราคา 30
  // → ทั้งเกมไม่มีวินาทีไหนที่ลงโซนแดงได้เลย จึงเปลี่ยนเป็นเส้นโค้ง "หน้าหนัก"
  apStart: 80,
  // จ่ายทุกต้นชั่วโมง — ใช้ค่าของช่วงแรกที่ยังไม่ถึง untilHour
  apCurve: [
    { untilHour: 24, ap: 25 }, // ช่วงที่โซนแดงยังมีชีวิต ต้องบุกให้ทัน
    { untilHour: 48, ap: 15 },
    { untilHour: 72, ap: 8 },
  ],

  // สถานะ จนท.
  injuredPenalty: -20,
  injuredRecoverHours: 6,
  lostConsciousHours: 9,
  criticalCountdownHours: 3,

  // %ช่วยชีวิตตอนสำเร็จ — ทอย 2 ชั้น (GAMESCREEN_SPEC §4.2)
  // ชั้น 2 สุ่มระดับจากตารางประจำตัว OP แล้วสุ่ม % ในช่วงของระดับนั้น — บัฟไม่มีผลกับชั้นนี้
  rescueTierChance: {
    human: { best: 20, high: 25, mid: 35, low: 20 }, // Robertson
    cat:   { best: 35, high: 30, mid: 25, low: 10 }, // Lyla
  },
  rescuePctRange: {
    best: [99, 99],
    high: [90, 98],
    mid:  [70, 89],
    low:  [50, 69],
  },

  // ค่าบัฟ (GAMESCREEN_SPEC §3.2)
  buffs: {
    crowd: { rate: 25, durationHours: 3, ap: 14, cooldownHours: 3, scope: 'zone' },
    scan:  { rate: 15, durationHours: 3, ap: 10, cooldownHours: 0, scope: 'zone',
             deathSlowFactor: 0.5, maxStacks: 5 },
    // Alert เดิม 35 AP แพงเกินกว่าจะคุ้ม เพราะบาดเจ็บแทบไม่เกิด — ตอนนี้อันตรายแรงขึ้นแล้ว
    // การเสีย จนท. ไป 6-9 ชม. กลางเกมคือหายนะ ประกันราคา 15 จึงคุ้มขึ้นมาจริง ๆ
    alert: { rate: 0,  durationHours: 3, ap: 15, cooldownHours: 3, scope: 'zone', safe: true },
    // Air Deploy เดิมอยู่แค่ 1 ชม. ราคา 24 → ไม่มีใครกดเลยสักครั้งใน 150 เกมที่จำลอง
    // ตอนนี้เป็น "ปุ่มฉุกเฉิน" — ชะลอการตายทั้งแมพ 40% ใช้ตอนหลายโซนใกล้หมดอายุพร้อมกัน
    air:   { rate: 15, durationHours: 3, ap: 20, cooldownHours: 4, scope: 'global',
             deathSlowFactor: 0.6 },
  },

  // สกิลลงพื้นที่ — AP และเวลาทำงานแยกตามระดับโซน (null = ลงโซนนั้นไม่ได้)
  // ตารางนี้คือ "แหล่งจริง" ของราคา/เวลา — data/operators.js อ่านค่าจากตรงนี้ ห้ามพิมพ์เลขซ้ำที่นั่น
  // Robertson เข้าโซนแดงได้แล้ว แต่ช้ากว่า Lyla และฐานสำเร็จต่ำกว่ามาก (75−40 = 35%)
  // ไม่บัฟคือส่งไปตาย — เป็นทางเลือกเสี่ยง ไม่ใช่ของแถม
  fieldSkills: {
    human: { base: 75, ap: { gray: 6,  yellow: 12, red: 20 }, workHours: { gray: 1, yellow: 2, red: 3 } },
    cat:   { base: 90, ap: { gray: 10, yellow: 18, red: 30 }, workHours: { gray: 1, yellow: 1, red: 2 } },
  },

  // Last Stand ของ Robertson (GAMESCREEN_SPEC §5)
  lastStand: {
    durationHours: 10,
    apCost: 0,                                        // ฟรีทุกสกิล
    workHours: { gray: 1, yellow: 2, red: 3 },        // เข้าโซนแดงได้ด้วย
    blockedSkills: ['crowd'],                         // Crowd Control ใช้ไม่ได้
    oncePerGame: true,
  },

  // ── คะแนนและแรงก์ (systems/score.js) ────────────────────────────
  //
  // ระบบเก่าคิดเป็น "ช่วยได้ − เสียชีวิต" ผลคือเล่นเก่งแค่ไหนก็ติดลบตลอด
  // เพราะช่วยได้เต็มที่ราว 37% ของเมือง อีก 63% นับเป็นลบเสมอ (−1,200 ถึง −300)
  // ระบบใหม่จึงให้คะแนนจาก "ทำได้ดีแค่ไหนเทียบกับที่ทำได้จริง" ไม่ใช่เทียบกับความสมบูรณ์แบบ
  //
  // คะแนนเต็ม 1,200 = น้ำหนัก 3 ส่วนรวมกัน แล้วคูณตัวคูณตอนจบ แล้วบีบให้อยู่ใน [min, max]
  score: {
    min: 10,      // เล่นแย่แค่ไหนก็ไม่ต่ำกว่านี้ (ลงสนามก็ได้แต้มความพยายาม)
    max: 1200,

    // ส่วนที่ 1 — จำนวนคนที่ช่วยได้ (ตัวหลัก)
    // เดิมตั้งเป้าไว้ 45% ผลคือช่วย 45% กับ 100% ได้คะแนนเท่ากันเป๊ะ (ตันทั้งคู่)
    // และช่วยแค่ 30% (ตาย 70%) ก็ได้แรงก์ A แล้ว — ไม่สมเหตุสมผล
    // ยกเป้าเป็น 60% ซึ่งอยู่เหนือเพดานที่วัดได้จริง (เล่นเก่งสุดราว 55%) เล็กน้อย
    // ส่วนนี้จึงไม่ตันในทางปฏิบัติ ช่วยได้มากขึ้นเท่าไหร่คะแนนขึ้นตามจริงเสมอ
    rescueWeight: 900,
    rescueTarget: 0.60,

    // ส่วนที่ 2 — กระจายไปได้กี่โซน (วัดความครอบคลุม ไม่ใช่แค่ตีโซนใหญ่โซนเดียว)
    // ลดน้ำหนักลงเพราะเมื่อคนกระจายสม่ำเสมอแล้ว การแตะให้ครบโซนทำได้ไม่ยาก
    // ถ้าให้น้ำหนักมากจะกลายเป็นแต้มแถมที่ดันแรงก์ขึ้นทั้งที่ช่วยคนได้น้อย
    zoneWeight: 150,
    zoneTarget: 0.85,   // ช่วยได้ 40 จาก 47 โซน = เต็มส่วนนี้

    // ส่วนที่ 3 — รักษาทีมไว้ได้แค่ไหน (เริ่มเต็ม แล้วหักตามจำนวนครั้งที่เจ็บ)
    safetyWeight: 150,
    injuryPenalty: 0.07,    // บาดเจ็บ 1 ครั้ง หัก 7% ของส่วนนี้
    blackoutPenalty: 0.15,  // หมดสติ 1 ครั้ง หัก 15%
    // ไม่ส่งใครออกไปเลย = ไม่มีใครเจ็บ ถ้าไม่กันไว้จะได้ 180 ฟรีจากการนั่งเฉย ๆ
    // ส่วนนี้จึงค่อย ๆ ปลดล็อกตามจำนวนภารกิจที่ส่งจริง ครบ 8 ครั้งถึงได้เต็ม
    safetyMinMissions: 8,

    // จบเพราะ CRITICAL (ภาคสนามล้มทั้งคู่) = ภารกิจล่ม คูณคะแนนที่ได้
    criticalMultiplier: 0.6,
  },

  // แรงก์ — เรียงจากสูงไปต่ำ ตัวแรกที่คะแนนถึงเกณฑ์คือแรงก์ที่ได้
  // เกณฑ์ยกขึ้นทั้งแถบ เพราะส่วนโซน+ความปลอดภัยรวมกัน 300 แต้มเป็นของที่เล่นพอเป็นก็ได้
  // ถ้าไม่ยก แรงก์สูง ๆ จะมาจากแต้มแถมแทนที่จะมาจากจำนวนคนที่ช่วยได้จริง
  ranks: [
    { key: 'S', min: 1090, title: 'ตำนานกู้ภัย',        note: 'ทั้งเมืองจะจดจำคืนนี้' },
    { key: 'A', min: 950,  title: 'ผู้บัญชาการชั้นเยี่ยม', note: 'ตัดสินใจแม่นแทบทุกครั้ง' },
    { key: 'B', min: 780,  title: 'ปฏิบัติการสำเร็จ',    note: 'ทำได้ดีกว่าที่หลายคนคาดไว้' },
    { key: 'C', min: 600,  title: 'พอไปวัดไปวาได้',     note: 'ช่วยคนได้จริง แต่ยังเหลือช่องให้ทำดีกว่านี้' },
    { key: 'D', min: 380,  title: 'ยังต้องฝึกอีกมาก',    note: 'ลองจัดลำดับโซนใหม่ แล้วใช้บัฟให้คุ้มกว่านี้' },
    { key: 'E', min: 0,    title: 'ภารกิจล้มเหลว',       note: 'เมืองต้องการคนที่กล้าตัดสินใจเร็วกว่านี้' },
  ],

  // เกมเริ่มด้วยสถานะเดินเวลา ไม่ได้เริ่มด้วยหยุด (GAMESCREEN_SPEC §2)
  startSpeed: 1,

  // ความเร็วเกม (มิลลิวินาทีจริงต่อ 1 ลูป)
  loopDurationMsBySpeed: { 1: 4200, 2: 2400 }, // ปกติ ~10 นาทีทั้งเกม / เร็ว ~5.8 นาที (144 ลูป)

  // จุดหยุดเวลาอัตโนมัติของเกม (Q3)
  // 30 = จุดทบทวนแผนกลางเกม · 54 = โซนแดงหมดอายุพอดี (ต้องตรงกับ zoneLifespanLoops
  // ไม่งั้น buildStops() หา tier ไม่เจอ แล้วป๊อปอัพจะขึ้นข้อความกลาง ๆ แทนที่จะบอกว่าโซนไหนหมดเวลา)
  autoPauseAtHour: [30, 54],
  autoPauseHoursLeft: 12,     // เตือนตอนเหลือ 12 ชม.สุดท้าย

  // เพลงในหน้าเกมสลับเป็นเพลงช่วงท้ายเมื่อเหลือเวลาในเกมน้อยกว่านี้ (ชั่วโมงในเกม)
  // ตรงกับจังหวะที่ป๊อปอัพ "เหลือเวลาอีก 12 ชั่วโมง" เด้ง — เข้าโหมดเร่งพร้อมกัน
  bgmFinalHours: 12,
};
